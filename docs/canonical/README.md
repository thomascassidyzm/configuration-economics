# Canonical nodes — one home per idea

One canonical statement per load-bearing idea; everything else references it and does not restate it (the never-twice rule applied to our own ideas). Correct an idea in its node and the correction propagates by citation.

Three nodes as of 2026-09-03:

- **[The selection primitive](the-selection-primitive.md)** — `docs/canonical/the-selection-primitive.md`. Ethics as selection over configurations, denominated against heat death; §1 RULED, the two-primitives block (distinction detects difference, selection orders it) FORMING, §6 (one primitive at two scales) awaiting Tom's ruling.
- **[The razor-blade case](../../src/content/open/NOTES-razor-blade-case.md)** — `src/content/open/NOTES-razor-blade-case.md`. Why possibility space is measured globally and trajectory-long, never locally; the one-term detector whose signature is a mixed sign pattern no lens can rescue; reproduced twice in the rig. It lives in `/open` rather than here because it was deliberately published as an open note; this index is its canonical pointer. *(Its GAP 7, saying the Jeremy Vine finding "is blocked on Tom's own account of it", predates the next node and is superseded by it — the source turned out to be Aran Jones's 2020 account, persisted in the estate on 2026-08-29.)*
- **[The Jeremy Vine finding](the-jeremy-vine-finding.md)** — `docs/canonical/the-jeremy-vine-finding.md`. FORMING. One sentence of Welsh (less than one, in fact) and, on Aran's testimony, a dampening effect on conflict arriving eighteen months later through people who were never present; the honest evidence ledger; the inventory of what a rig would need before the mechanism could be expressed, closing on the §13(b) conflict-channel fork that is Tom's to rule.

**Split convention, flagged rather than settled:** two nodes live in this directory, one in `src/content/open/`. Whether canonical nodes should converge on one home is Tom's call; until then this index is the single place that names them all.
