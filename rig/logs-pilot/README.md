The 30-tick pilot (and the first ~17 ticks of the aborted first matrix attempt, which was
launched as a session-owned background task and died with the session). Kept for
provenance: the pooled omega variant was decided on this evidence, before the main runs.
These logs predate the job #216 audit diagnostics, so they carry no
sum_strict/sum_realizable fields. The main-run logs are in rig/logs/.
