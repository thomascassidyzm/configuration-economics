---
// Configuration Economics - Living Epistemic Work
// Index page with navigation to concept explorations
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />
    <title>Configuration Economics — A Living Epistemic Work</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,500;1,400&family=Sora:wght@300;400;500&family=JetBrains+Mono:wght@400&display=swap" rel="stylesheet">
    <style>
      :root {
        --void: #0a0a0c;
        --depth-1: #12121a;
        --text-primary: #e8e8f0;
        --text-secondary: #a0a0b8;
        --text-muted: #606080;
        --accent: #4a9eff;
        --font-display: 'Cormorant Garamond', serif;
        --font-body: 'Sora', sans-serif;
        --font-mono: 'JetBrains Mono', monospace;
      }

      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        font-family: var(--font-body);
        background: var(--void);
        color: var(--text-primary);
        min-height: 100vh;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        padding: 2rem;
      }

      .container {
        max-width: 600px;
        text-align: center;
      }

      h1 {
        font-family: var(--font-display);
        font-size: 3rem;
        font-weight: 300;
        margin-bottom: 0.5rem;
      }

      .subtitle {
        font-size: 0.875rem;
        color: var(--text-muted);
        letter-spacing: 0.2em;
        text-transform: uppercase;
        margin-bottom: 3rem;
      }

      .status {
        font-family: var(--font-mono);
        font-size: 0.75rem;
        color: var(--text-muted);
        margin-bottom: 3rem;
        padding: 0.5rem 1rem;
        border: 1px solid rgba(255,255,255,0.1);
        border-radius: 4px;
        display: inline-block;
      }

      .links {
        display: flex;
        flex-direction: column;
        gap: 1rem;
        margin-bottom: 3rem;
      }

      a {
        color: var(--text-secondary);
        text-decoration: none;
        padding: 1rem 2rem;
        border: 1px solid rgba(255,255,255,0.1);
        border-radius: 8px;
        transition: all 0.3s ease;
      }

      a:hover {
        border-color: var(--accent);
        color: var(--text-primary);
        transform: translateY(-2px);
      }

      a.primary-link {
        border-color: var(--accent);
        background: rgba(74, 158, 255, 0.08);
      }

      a.primary-link:hover {
        background: rgba(74, 158, 255, 0.15);
      }

      .note {
        font-size: 0.875rem;
        color: var(--text-muted);
        line-height: 1.6;
        max-width: 500px;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1>Configuration Economics</h1>
      <p class="subtitle">A Living Epistemic Work</p>

      <span class="status">Development Preview</span>

      <div class="links">
        <a href="/essay" class="primary-link">Essay 1: Value under Physical Constraint</a>
        <a href="/concept-dark">Format Exploration — Dark</a>
        <a href="/concept-light">Format Exploration — Light</a>
      </div>

      <p class="note">
        A living epistemic work where content can be read at multiple resolutions,
        each section carries its epistemic status, and an embedded guide helps
        navigate the framework at your own depth.
      </p>
    </div>
  </body>
</html>
